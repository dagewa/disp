Source tarball created with

	R CMD build DISP

on Ubuntu laptop, running R 2.14.0


Windows binary zip file created with

	R CMD INSTALL --resave-data --build DISP_0.1-3.tar.gz

in a command prompt under Windows 7 with R 2.15.2. Both 32 and 64 bit versions of compiled code are included.

NB. R CMD INSTALL --resave-data --build DISP (i.e on the source directly, not a tarball) *did not work*
